<UserControl x:Class="AssetTracker.Views.AddAsset"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:AssetTracker.Views"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="600"
             Width="600"
             d:DataContext="{d:DesignInstance Type=local:AddAssetViewModel}">
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Label Content="Item Name" />
            <TextBox Grid.Column="1" Grid.Row="0" Margin="5" Text="{Binding ItemName}"/>
            <Label Grid.Column="0" Grid.Row="1" Content="Item Description" />
            <TextBox Grid.Row="1" Grid.Column="1" AcceptsReturn="True" TextWrapping="Wrap"
                     Height="200" ScrollViewer.VerticalScrollBarVisibility="Auto"  HorizontalAlignment="Stretch" 
                     VerticalAlignment="Top" Margin="5" Text="{Binding ItemDescription}"/>
            <Button Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Command="{Binding SaveDataCommand}">SAVE</Button>
        </Grid>
    </Grid>
</UserControl>
